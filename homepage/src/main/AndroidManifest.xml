<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.homepage">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.HomeActivity"/>
        <activity android:name=".activity.HomeViewPagerActivity"/>
        <activity android:name=".activity.TabComplexActivity"/>
        <activity android:name=".activity.HomePageActivity"/>
        <activity android:name=".activity.SVLisActivity"/>
        <activity android:name=".activity.HH"/>
        <activity android:name=".recyclerview.GroupLayoutActivity"/>
        <activity android:name=".recyclerview.ExpandableUseActivity"/>
    </application>

</manifest>